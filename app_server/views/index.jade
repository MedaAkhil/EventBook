extends layout

block content
  .container
    h2 Home Page
    if user
      p Welcome, #{user.username}!
      a(href="/logout") Logout
    else
      p You are not logged in.
      a(href="/signin") -Login-
 